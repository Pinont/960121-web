Generate register.js (logic) and express.js (route) with comments explaining each step. User registration: form takes name, email, password → frontend validates (min 8 chars, 1 uppercase, 1 special char !@#$%^&*) → POST /register checks auth_user.json for duplicate email (409) or appends {id:uuid, name, username:email, password:bcrypt, created_at, updated_at} (201). Serve public/index.html on port 3000. No frameworks, use fs for JSON.